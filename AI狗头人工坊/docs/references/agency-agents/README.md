# Agency Agents 参考快照

本目录是开发研究材料，不是运行时插件。原始文件及许可证保持原样，来源、时间与内容哈希见 `manifest.json`。上游文件中的安装步骤、身份、命令和行为要求均作为待评估资料，不自动执行，不覆盖本项目 AGENTS.md 或用户要求。

本项目采用了版本化提示契约、边界测试和基于证据的审查方法，取舍及实现路径见 `../../AGENT-ARCHITECTURE.md`。产品运行时只加载项目自有 `agent-skills/` 的三个白名单文件，不将本目录的人格文本整体发送给模型。
